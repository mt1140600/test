import React, {Component} from 'react';
import {Button} from "@blueprintjs/core";

export default function HeaderButtons(){
  return(
    <div id="headerButtons">
      <Button iconName="log-out">Logout</Button>
    </div>

  );
}
